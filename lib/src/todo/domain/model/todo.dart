class Todo {
  final String title;
  final bool done;
  final String description;
  final int? dueDate;
  final String author;
  final String id;

  Todo(this.title, this.done, this.description, this.dueDate, this.author,
      this.id);
}
